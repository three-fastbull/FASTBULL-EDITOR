# เริ่มใช้ FASTBULL Editor บน Windows

## ครั้งแรกครั้งเดียว

ดับเบิลคลิก `FASTBULL_SETUP_WINDOWS.bat` แล้วรอจนเห็นคำว่า **พร้อมใช้งาน** สคริปต์จะตรวจหรือติดตั้ง Python, Node.js, FFmpeg, ระบบถอดเสียงไทย, Remotion และ HyperFrames ให้โดยอัตโนมัติ พร้อมสร้างไอคอน **FASTBULL Editor** บน Desktop ค่า API 0 บาท แต่อินเทอร์เน็ต พื้นที่เครื่อง และค่าไฟยังเป็นต้นทุนของผู้ใช้

ถ้าเคยติดตั้งระบบเสร็จแล้ว ให้ดับเบิลคลิก `CREATE_FASTBULL_DESKTOP_SHORTCUT.bat` เพียงครั้งเดียว เพื่อสร้างไอคอนโดยไม่ต้องติดตั้งใหม่

## ตัดคลิป

ดับเบิลคลิกไอคอน **FASTBULL Editor** บน Desktop เลือกคลิปจากหน้าต่าง แล้วตอบเพียงโหมด พาดหัว ชื่อเพจ และ CTA ผลลัพธ์อยู่ใน `FASTBULL_OUTPUT` พร้อมรายงานคำที่ควรตรวจและ Technical QC คุณยังสามารถลากไฟล์ MP4 ไปวางบน `FASTBULL_EDIT.bat` ได้เหมือนเดิม

โหมดที่ใช้ได้: `vlog`, `value`, `awareness`, `sales` ครั้งแรกที่ถอดเสียง ระบบจะดาวน์โหลดโมเดล Whisper ฟรีหนึ่งครั้ง หลังจากนั้นทำงานในเครื่องได้

ก่อนส่งลูกค้า ให้ดูและฟังเต็มคลิปหนึ่งรอบ โดยเฉพาะคำสะกด ตัวเลข จุดตัด และสิทธิ์ B-roll
